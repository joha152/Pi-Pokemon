import React from "react";
import {filterTypes, getTypes} from "../../redux/actions/actions";
import styles from "./FilterType.module.css";


const FilterType = (props)=>{
  return(
    <div>Esto es el filtro por tipo </div>
  )
}

export default FilterType;